"""Analytics over approved movements: trends, timeline, AML patterns, reconciliation.

Pure Python on `Transaction` value objects, with `Decimal` for money. The application
layer decides which movements are analysed (by default, those of approved batches).
"""
